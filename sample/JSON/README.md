# JSON 中文命名示例

## 环境准备

请先安装本插件（无需安装 JSON 语言支持插件（VSCode 原生支持））

## 定义 Schema

- 根据 [JSON Schema](https://json-schema.fullstack.org.cn/docs) 规范文档，编写中文的文档结构、约束和数据类型定义
- （如：中文属性名、中文枚举项）

## 编写 JSON

- 定义 Schema 后，就可以使用中文属性名、中文枚举项了。
- 本插件将为您提供：中文标识符的快速自动补全。
