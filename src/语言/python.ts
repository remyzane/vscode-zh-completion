import { 语言 } from '.';

export const python: 语言 = {
    补全锚点配置: {
        是否标识符字符: (字符) => /[a-zA-Z0-9_$\u4e00-\u9fa5]/.test(字符),
        语法边界字符: new Set(['.', ',', '(', '[', '{', ':']),
        支持模板表达式: true,
        最大回退距离: 30
    }
};
