import { 语言基类 } from '../基类';


export class 语言实现 extends 语言基类 {

    constructor() {
        super();
        this.触发字符 = [
            '"',    // 字符串键起始（如 "key"），部分 LSP 在输入 " 后预填充键名
            ':',    // 键值分隔符（"key": ），触发值的补全（如字符串、布尔、对象等）
            ',',    // 对象/数组元素分隔（"a": 1, ），触发下一个键或数组项的补全
            '{',    // 对象字面量开始（{ ），触发顶层键建议（需 $schema）
            '[',    // 数组字面量开始（[ ），触发数组项结构建议
            '\n',   // 换行（在对象/数组内换行后可能触发新成员补全）
            '\r',   // 回车（兼容 Windows 换行）
        ];
        // this.补全锚点配置 = {
        //     是否标识符字符: (字符) => /[a-zA-Z0-9_]/.test(字符), // JSON key 通常为简单标识符
        //     最大回退距离: 15,
        // };
    }
}

export default new 语言实现();
